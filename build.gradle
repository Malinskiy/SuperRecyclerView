buildscript {
  ext.deps = [androidSupport: "23.2.0"]
  ext.setup = [buildTools: "23.0.2"]

  repositories {
    jcenter()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:2.0.0-beta6'
  }
}

allprojects {
  repositories {
    jcenter()
  }
}
